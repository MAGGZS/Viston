# ============================================================
# FRONTEND — Variáveis de Ambiente
# Copie para .env.local (desenvolvimento) ou configure na Vercel (produção).
# ============================================================

# LOCAL: aponta para o backend rodando localmente
NEXT_PUBLIC_API_URL="http://localhost:3001/api"

# PRODUÇÃO: substitua pela URL do seu serviço no Render
# NEXT_PUBLIC_API_URL="https://viston-api.onrender.com/api"
