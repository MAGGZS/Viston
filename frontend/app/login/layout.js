export const metadata = {
  title: 'Entrar',
  description: 'Acesse sua conta Viston para registrar vistorias e consultar relatórios.',
  alternates: { canonical: '/login' },
};

export default function LoginLayout({ children }) {
  return children;
}
