export const metadata = {
  title: 'Criar conta',
  description:
    'Crie sua conta no Viston. Depois de entrar, peça a chave do prédio ao administrador para começar a vistoriar.',
  alternates: { canonical: '/register' },
};

export default function RegisterLayout({ children }) {
  return children;
}
